vim.fn["signature_help#enable"]()
vim.g.signature_help_config = {
  border = true,
  maxWidth = 80,
  maxHeight = 30,
  style = "virtual",
  onTriggerChar = false,
  multiLabel = false,
  fallbackToBelow = true,
}
