options(repos="http://cran.rstudio.com/")
options(warnPartialMatchAttr=TRUE, warnPartialMatchDollar=TRUE)
options(warn=1,showWarnCalls=TRUE, showErrorCalls=TRUE)

.First = function(){
}

.Last = function(){
    if (interactive()) try(savehistory())
}

